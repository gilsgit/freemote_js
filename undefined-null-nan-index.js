//  undefined, null, NaN

let name;

// undefined
console.log(name);

const user = {
    name: 'Aaron', 
    age: null,    // placeholder cause don't know yet
    ccNum: null // say a user doesn't want info there

}

console.log(user.age);

// NaN (not a number)
console.log(10 + user.address);

